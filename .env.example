# Railway Environment Variables Template
# Copy these to Railway Dashboard → Variables → Shared Variables

# PostgreSQL superuser password (SCRAM-SHA-256)
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# Repmgr user password (used for cluster management and replication)
REPMGR_PASSWORD=CHANGE_ME_REPMGR_PASSWORD

# Application read-only user password
APP_READONLY_PASSWORD=CHANGE_ME_READONLY_PASSWORD

# Application read-write user password
APP_READWRITE_PASSWORD=CHANGE_ME_READWRITE_PASSWORD

# PgPool admin (PCP protocol) password
PCP_PASSWORD=CHANGE_ME_PCP_ADMIN_PASSWORD

# ============================================================
# IMPORTANT SECURITY NOTES:
# ============================================================
# 1. NEVER commit actual passwords to git
# 2. Use strong, unique passwords (16+ chars, mixed case, numbers, symbols)
# 3. Set these as SHARED variables in Railway (not per-service)
# 4. Railway will encrypt these at rest
# 5. Generate via: openssl rand -base64 32
# ============================================================

# Optional: Advanced tuning (defaults shown)
# REPMGR_PROMOTE_MAX_LAG_SECS=10
# PGPOOL_WAIT_RETRIES=600
# PGPOOL_WAIT_INTERVAL=5
# EXPORT_PLAINTEXT_POOLPWD=false
